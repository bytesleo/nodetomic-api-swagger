/auth/admin/size:
  x-swagger-router-controller: redis.admin
  # logout
  get:
    operationId: size
    security:
      - Bearer: []
    x-security-scopes:
      - admin
    tags:
      - Admin Redis
    summary: Get count sessions [Role Admin]
    description: Get count sessions
    responses:
      200:
        description: Success
      401:
        description: Unauthenticated
      403:
        description: Forbidden
      404:
        description: Not found
      500:
        description: Error
#---------------------------------------------
/auth/admin/info:
  x-swagger-router-controller: redis.admin
  # logout
  get:
    operationId: info
    security:
      - Bearer: []
    x-security-scopes:
      - admin
    consumes:
      - text/html
    produces:
      - text/html
    tags:
      - Admin Redis
    summary: Get info redis [Role Admin]
    description: Get info redis
    responses:
      200:
        description: Success
      401:
        description: Unauthenticated
      403:
        description: Forbidden
      404:
        description: Not found
      500:
        description: Error
