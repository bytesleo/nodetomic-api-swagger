/api/admin/user:
  x-swagger-router-controller: userAdmin
# list
  get:
    operationId: list
    security:
      - Bearer: []
    x-security-scopes:
      - admin
    tags:
      - User
    summary: Get the list of users [Role admin]
    description: Get the list of users [Role admin]
    responses:
      200:
        description: Success.
        schema:
          type: array
          items:
            $ref: "#/definitions/User"
      401:
        $ref: '#/responses/401'
      403:
        $ref: '#/responses/403'
      404:
        $ref: '#/responses/404'
      500:
        $ref: '#/responses/500'

/api/admin/user/{id}:
  x-swagger-router-controller: userAdmin
# destroy
  delete:
    operationId: destroy
    security:
      - Bearer: []
    x-security-scopes:
      - admin
    tags:
      - User
    summary: Remove a user by id [Role admin]
    description: Remove a user by id [Role admin]
    parameters:
      - name: id
        in: path
        required: true
        type: string
    responses:
      200:
        $ref: '#/responses/200'
      401:
        $ref: '#/responses/401'
      403:
        $ref: '#/responses/403'
      404:
        $ref: '#/responses/404'
      500:
        $ref: '#/responses/500'
# update
  put:
    operationId: update
    security:
      - Bearer: []
    x-security-scopes:
      - admin
    tags:
      - User
    summary: Update a user by id [Role admin]
    description: Update a user by id [Role admin]
    parameters:
      - name: id
        in: path
        required: true
        type: string
      - in: body
        name: body
        schema:
          type: array
          $ref: "#/definitions/User"
    responses:
      200:
        description: Success.
        schema:
          type: object
          $ref: "#/definitions/User"
      401:
        $ref: '#/responses/401'
      403:
        $ref: '#/responses/403'
      404:
        $ref: '#/responses/404'
      500:
        $ref: '#/responses/500'